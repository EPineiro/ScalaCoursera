package forcomp


object Anagrams {
  
  import forcomp.Anagrams._;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(106); 
  
  val o = wordOccurrences("Emanuel");System.out.println("""o  : <error> = """ + $show(o ))}
}
